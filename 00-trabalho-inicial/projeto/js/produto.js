const QUANTIDADE = document.querySelector('#quantidade'); const SPAN = document.querySelector('#valor'); QUANTIDADE.addEventListener('change', function () { const quantidade = parseInt(QUANTIDADE.value); SPAN.textContent = 'R$ ' + (quantidade * 10) + ',00' })
